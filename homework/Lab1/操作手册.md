# Lab1 安装手册：VMware 与 Ubuntu 虚拟机

## 一、实验背景

本课程的实验需要在 Linux 系统中执行命令、查看日志、安装服务和修改配置。直接在自己的 Windows 主机上做这些操作不现实，也不安全：Windows 和 Linux 的命令体系不同，误操作还可能影响自己的日常电脑。

虚拟机的作用就是在一台真实电脑里再运行一台“虚拟电脑”。你可以把 Ubuntu 安装在虚拟机中，像使用一台独立 Linux 主机一样操作它；即使实验配置写错、服务启动失败，也不会破坏宿主机。必要时还可以删除虚拟机重新安装。

本实验最终要得到一台能够正常启动、联网、打开终端并执行 Linux 命令的 Ubuntu 虚拟机。

---

## 二、准备文件

### 课程仓库中的文件

本目录（`homework/Lab1/`）只包含两个文档：

```text
homework/Lab1/
├── 操作手册.md
└── Lab1.md
```

| 文件 | 作用 | 说明 |
| :--- | :--- | :--- |
| `操作手册.md` | 本安装手册 | 按顺序完成即可 |
| `Lab1.md` | Lab1 验收作业 | 完成安装后填写并提交 |

### 教师通过企业微信发送的文件

VMware 安装包和 Ubuntu 镜像体积很大，不放在课程仓库中，由教师通过**企业微信**发送。请自行接收并保存到宿主机上一个方便查找的目录，例如 `D:\LogLab\`：

| 文件 | 作用 |
| :--- | :--- |
| `VMware-Workstation-Full-26H1-25388281.exe` | VMware Workstation Pro Windows 安装包，用于在 Windows 上安装虚拟机软件 |
| `ubuntu-24.04.4-desktop-amd64.iso` | Ubuntu Desktop 安装镜像，用于创建 Ubuntu 虚拟机 |

> **注意**：这两个文件不要放进课程仓库的文件夹，也不要提交到 Git。仓库只用于提交作业文档和截图，提交大文件会导致 PR 无法正常检查。

### 版本选择说明

- VMware 选择 **VMware Workstation Pro 26H1 for Windows**。
- Ubuntu 选择 **Ubuntu 24.04.4 LTS Desktop amd64**。

选择 Ubuntu 24.04 LTS 的原因：LTS 是长期支持版本，稳定性更适合教学实验；24.04.x 版本的软件包、命令和日志路径也比较一致。

### 官方下载地址

如果企业微信中的文件已过期或接收失败，可从以下官方入口下载：

- VMware Workstation Pro：<https://support.broadcom.com/group/ecx/productdownloads?subfamily=VMware+Workstation+Pro>
- Ubuntu Desktop ISO：<https://releases.ubuntu.com/24.04/>

> **注意**：VMware 当前由 Broadcom 分发，官方门户可能要求注册或登录。优先使用教师通过企业微信发送的安装包，可以省去注册和排队下载的时间。

---

## 三、宿主机要求与虚拟机配置

开始前先确认自己的电脑满足以下要求：

| 项目 | 最低要求 | 建议配置 |
| :--- | :--- | :--- |
| 操作系统 | Windows 10/11 64 位 | Windows 11 64 位 |
| 内存 | 8GB | 16GB 或以上 |
| 磁盘剩余空间 | 40GB | 80GB 或以上 |
| CPU | 支持虚拟化 | Intel VT-x 或 AMD-V 已开启 |
| 网络 | 可访问互联网 | 安装和更新软件包会用到 |

### 根据宿主机选择虚拟机配置

先在 Windows 的“任务管理器 → 性能”中查看宿主机的 CPU 核心数和内存，再在“此电脑”中查看存放虚拟机的磁盘剩余空间。按下表选择与自己宿主机匹配的档位：

| 宿主机情况 | 虚拟 CPU 总核心数 | 虚拟内存 | 虚磁盘上限 | 适用说明 |
| :--- | :---: | :---: | :---: | :--- |
| 8GB 内存，4 核左右，剩余 50GB 以上 | 2 核 | 4096MB | 40GB | 最低可用档；做实验时关闭宿主机上不必要的程序 |
| 16GB 内存，6 核以上，剩余 80GB 以上 | 2 核 | 6144MB | 60GB | **课程推荐档**；能较流畅运行 Ubuntu 桌面和日志平台 |
| 32GB 及以上内存，8 核以上，剩余 120GB 以上 | 4 核 | 8192MB | 80GB | 宽裕档；适合长期保留日志和多个快照 |

> **选择规则**：宿主机同时满足该行的内存、CPU 和磁盘条件时，才选择该档。任意一项不满足时，选低一档。不要把宿主机的全部 CPU 或内存分给虚拟机，否则 Windows 和虚拟机都会变卡。

虚磁盘默认按实际使用量逐步增长，将上限设为 60GB 或 80GB 不代表创建时立即占用同等大小。但 VMware 快照会额外占用宿主机空间，学期内应定期检查剩余空间。

### 检查 CPU 虚拟化

1. 在 Windows 中按 `Ctrl + Shift + Esc` 打开任务管理器。
2. 点击 **性能**。
3. 选择 **CPU**。
4. 查看右下角是否显示 **虚拟化：已启用**。

如果显示 **虚拟化：已禁用**，需要进入 BIOS/UEFI 开启 Intel VT-x、Intel Virtualization Technology、AMD-V 或 SVM Mode。不同品牌电脑入口不同，常见按键是 `F2`、`F10`、`F12`、`Del` 或 `Esc`。

---

## 四、安装 VMware Workstation Pro

### 步骤 1：运行安装程序

1. 打开保存安装包的文件夹，例如 `D:\LogLab\`。
2. 双击 `VMware-Workstation-Full-26H1-25388281.exe`。
3. 如果 Windows 弹出“是否允许此应用对设备进行更改”，点击 **是**。
4. 等待安装向导加载。

### 步骤 2：按向导安装

在安装向导中按以下顺序操作：

1. 点击 **Next**。
2. 勾选 **I accept the terms in the License Agreement**，点击 **Next**。
3. 安装位置保持默认即可，点击 **Next**。
4. 如果看到 **Enhanced Keyboard Driver**，可不勾选；普通实验不需要。
5. **Check for product updates on startup** 可取消勾选，避免每次启动检查更新。
6. **Join the VMware Customer Experience Improvement Program** 可取消勾选。
7. 快捷方式保持默认，点击 **Next**。
8. 点击 **Install** 开始安装。
9. 安装完成后点击 **Finish**。

### 步骤 3：首次启动

1. 从桌面或开始菜单打开 **VMware Workstation Pro**。
2. 如果出现许可证或使用类型页面，按学校或教师说明选择。
3. 能看到 VMware 主界面，即表示安装完成。

---

## 五、创建 Ubuntu 虚拟机

### 步骤 1：新建虚拟机

1. 打开 VMware Workstation Pro。
2. 点击 **Create a New Virtual Machine**。
3. 选择 **Typical (recommended)**。
4. 点击 **Next**。

### 步骤 2：选择 Ubuntu 镜像

1. 选择 **Installer disc image file (iso)**。
2. 点击 **Browse**。
3. 选择保存好的 `ubuntu-24.04.4-desktop-amd64.iso`。
4. 点击 **Next**。

如果 VMware 识别出 Ubuntu，并提示 Easy Install，可以继续；如果没有识别，也不影响，后面进入 Ubuntu 安装界面手动安装即可。

### 步骤 3：填写虚拟机信息

建议填写：

| 项目 | 建议值 |
| :--- | :--- |
| Full name | `姓名拼音` |
| User name | `姓名拼音` |
| Password | 自己能记住的密码 |
| Virtual machine name | `Ubuntu-24.04-LogLab` |
| Location | 保持默认，或选择剩余空间充足的磁盘 |

> **注意**：用户名建议只使用小写字母和数字，不要使用中文、空格或特殊符号。

### 步骤 4：设置磁盘

1. 根据第三节的宿主机档位，磁盘大小填写 **40GB、60GB 或 80GB**。
2. 选择 **Store virtual disk as a single file** 或 **Split virtual disk into multiple files** 都可以。
3. 点击 **Next**。

说明：

- 这里填写的是虚磁盘上限，不代表立刻占用同等大小的宿主机空间。
- 如果经常复制虚拟机文件，选择 split 更方便。
- 如果追求性能和文件整洁，选择 single file 更简单。

### 步骤 5：自定义硬件

在最后确认页点击 **Customize Hardware**，根据第三节选定的档位设置。下表为课程推荐档：

| 硬件 | 建议值 |
| :--- | :--- |
| Memory | 6144 MB；宿主机只有 8GB 内存时改为 4096MB |
| Processors | 2 个核心 |
| Network Adapter | NAT |
| CD/DVD | 使用 `ubuntu-24.04.4-desktop-amd64.iso` |
| Display | 保持默认 |

设置完成后点击 **Close**，再点击 **Finish**。

> VMware 可能将 CPU 分成 **Number of processors** 和 **Number of cores per processor** 两项。本课程建议前者设为 `1`，2 核档将后者设为 `2`，4 核档将后者设为 `4`。两者相乘才是 Ubuntu 中 `nproc` 看到的总核心数。

---

## 六、安装 Ubuntu

### 步骤 1：启动虚拟机

1. 选中刚创建的 `Ubuntu-24.04-LogLab`。
2. 点击 **Power on this virtual machine**。
3. 等待进入 Ubuntu 安装界面。

如果出现黑屏或卡住，先等 2～3 分钟；仍无响应再关闭虚拟机重新启动。

### 步骤 2：选择安装语言

建议选择：

- 语言：**English**
- 键盘：**English (US)**

说明：系统界面可以使用英文，遇到报错、日志、命令输出时更容易搜索和对照。

### 步骤 3：选择安装方式

在 Ubuntu 安装器中按以下选择：

1. 选择 **Install Ubuntu**。
2. 选择 **Interactive installation**。
3. 应用集合选择 **Default selection**。
4. 第三方软件可暂时不勾选。
5. 磁盘设置选择 **Erase disk and install Ubuntu**。

> **重要**：这里擦除的是虚拟机里的虚拟磁盘，不是 Windows 真实硬盘。只要你是在 VMware 虚拟机窗口里操作，就不会格式化宿主机磁盘。

### 步骤 4：创建用户

建议：

| 项目 | 建议值 |
| :--- | :--- |
| Your name | `姓名拼音` |
| Your computer's name | `随意` |
| Pick a username | `姓名拼音` |
| Password | 自己能记住的密码 |
| Require my password to log in | 勾选 |

### 步骤 5：选择时区并安装

1. 时区选择 **Shanghai**。
2. 点击 **Install**。
3. 等待安装完成，通常需要 10～30 分钟。
4. 提示重启时点击 **Restart Now**。
5. 如果提示移除安装介质，直接按 `Enter`。

---

## 七、安装后的基础检查

登录 Ubuntu 后，按 `Ctrl + Alt + T` 打开终端。下面的检查请**一条一条执行**，每执行一条就先看清它的输出，再执行下一条。不要把所有命令一次性粘贴进终端，否则输出会混在一起，看不出是哪条命令出了问题。

### 检查 1：确认 Ubuntu 版本

```bash
cat /etc/os-release
```

期望在输出中看到 `PRETTY_NAME="Ubuntu 24.04.x LTS"`。安装后如果执行过系统更新，小版本号可能高于 `24.04.4`，这属于正常现象。

### 检查 2：确认主机名

```bash
hostname
```

期望输出 `ubuntu`，或者你在安装时为计算机填写的名称。

### 检查 3：确认 CPU 核心数

```bash
nproc
```

期望输出与第三节选定的档位一致，即 `2` 或 `4`。

### 检查 4：确认内存

```bash
free -h
```

看 `Mem` 行的 `total` 列。显示值会略小于 VMware 中设置的数值，因为一部分内存被固件和内核占用：4GB 档约显示 `3.8Gi`，6GB 和 8GB 档同样会略小于设置值。

### 检查 5：确认根分区容量

```bash
df -h /
```

看 `Size` 列。容量可能略小于选定的 40GB、60GB 或 80GB 虚磁盘上限，属于正常现象。

### 检查 6：确认 IP 地址

```bash
ip addr
```

期望在 `ens33`（或类似名称）的网卡下看到一个以 `192.168`、`172` 或 `10` 开头的 IPv4 地址。只有 `lo` 回环网卡的 `127.0.0.1` 说明虚拟机还没有拿到 IP。

### 检查 7：测试 IP 联通性

```bash
ping -c 4 223.5.5.5
```

能看到 4 行返回时间，说明虚拟机可以通过 NAT 访问外部网络。

### 检查 8：测试 DNS 解析

```bash
ping -c 4 mirrors.tuna.tsinghua.edu.cn
```

能解析出 IP 并看到返回时间，说明 DNS 解析和域名访问都正常。

> `ping` 默认会一直运行，`-c 4` 表示只发送 4 个包后自动停止。如果忘记加 `-c` 参数，按 `Ctrl + C` 可以随时停止。

---

## 八、配置国内软件源

### 为什么要做这一步

Ubuntu 默认从官方源（`archive.ubuntu.com`）下载软件包。在中国大陆访问官方源经常很慢，甚至连接超时，会导致后面的 `sudo apt update` 和 `sudo apt install` 长时间卡住或直接失败。把软件源换成国内镜像站之后，下载速度通常有明显提升。

本手册使用**清华大学开源软件镜像站（TUNA）**。

### 步骤 1：备份原有配置

Ubuntu 24.04 的软件源配置文件是 `/etc/apt/sources.list.d/ubuntu.sources`，采用新的 DEB822 格式，不再使用旧版的 `/etc/apt/sources.list`。网上很多针对 Ubuntu 22.04 及更早版本的换源教程改的是旧文件，在 24.04 上改了不生效。

先备份：

```bash
sudo cp /etc/apt/sources.list.d/ubuntu.sources /etc/apt/sources.list.d/ubuntu.sources.bak
```

如果后面配置写错了，用这条命令还原：

```bash
sudo cp /etc/apt/sources.list.d/ubuntu.sources.bak /etc/apt/sources.list.d/ubuntu.sources
```

### 步骤 2：写入清华镜像源

把下面**整段内容**复制粘贴到终端后按回车。注意要从第一行的 `sudo tee` 一直复制到最后一行的 `EOF`，中间不能漏行：

```bash
sudo tee /etc/apt/sources.list.d/ubuntu.sources > /dev/null <<'EOF'
Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/ubuntu
Suites: noble noble-updates noble-backports
Components: main restricted universe multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

Types: deb
URIs: https://mirrors.tuna.tsinghua.edu.cn/ubuntu
Suites: noble-security
Components: main restricted universe multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
EOF
```

说明：

- `noble` 是 Ubuntu 24.04 的代号。如果你装的是其他版本的 Ubuntu，必须把 `noble` 换成对应代号，否则更新时会报找不到软件源。
- `Signed-By` 指向系统自带的官方签名密钥，用于校验从镜像站下载的软件包，不要删除这一行。

### 步骤 3：确认写入的内容

```bash
cat /etc/apt/sources.list.d/ubuntu.sources
```

输出应与上一步粘贴的内容一致，`URIs` 两行都指向 `mirrors.tuna.tsinghua.edu.cn`。

### 步骤 4：刷新软件包索引

```bash
sudo apt update
```

正常时输出中的 `Get:` 行都来自 `mirrors.tuna.tsinghua.edu.cn`，最后显示读取完成，并且没有 `Err:`、`Failed` 或 `Could not resolve` 之类的错误。

### 备用镜像站

如果清华源当时较慢或无法访问，可以把上面命令中的 `URIs` 换成下面任意一个，其余内容保持不变：

| 镜像站 | `URIs` 填写值 |
| :--- | :--- |
| 清华大学 TUNA | `https://mirrors.tuna.tsinghua.edu.cn/ubuntu` |
| 阿里云 | `https://mirrors.aliyun.com/ubuntu` |
| 中国科学技术大学 | `https://mirrors.ustc.edu.cn/ubuntu` |

---

## 九、安装课程必需组件

本课程要求安装 VMware Tools、OpenSSH Server 和 rsyslog：

- **VMware Tools**：改善分辨率、自适应窗口、复制粘贴和鼠标体验。
- **OpenSSH Server**：为后续 SSH 登录、认证日志和 fail2ban 实验提供环境。
- **rsyslog**：将系统和认证事件写入 `/var/log/syslog`、`/var/log/auth.log` 等文本日志，是后续实验的基础。

请先完成第八节的换源，再执行本节命令，否则下载可能非常慢。

### 步骤 1：安装软件包

```bash
sudo apt install -y open-vm-tools open-vm-tools-desktop openssh-server rsyslog
```

### 步骤 2：设置服务开机自启并立即启动

```bash
sudo systemctl enable --now ssh rsyslog
```

`enable` 表示开机自动启动，`--now` 表示同时立刻启动，两件事一条命令完成。

### 步骤 3：重启虚拟机

```bash
sudo reboot
```

VMware Tools 的桌面集成功能需要重启后才会生效。

### 步骤 4：逐条验证

重启并重新登录后，打开终端，同样**一条一条执行**下面的命令：

**验证 1：VMware Tools 版本**

```bash
vmware-toolbox-cmd -v
```

期望输出一个版本号，例如 `12.x.x.xxxxx (build-xxxxxxx)`。

**验证 2：`open-vm-tools` 服务是否在运行**

```bash
systemctl is-active open-vm-tools
```

期望输出 `active`。

**验证 3：SSH 和 rsyslog 是否已设为开机自启**

```bash
systemctl is-enabled ssh rsyslog
```

期望输出两行 `enabled`。

**验证 4：SSH 和 rsyslog 是否正在运行**

```bash
systemctl is-active ssh rsyslog
```

期望输出两行 `active`。

**验证 5：是否在监听 22 端口**

```bash
ss -lnt | grep ':22 '
```

期望看到一行包含 `LISTEN` 和 `:22` 的输出。没有任何输出说明 SSH 没有正常监听。

**验证 6：rsyslog 能否写入日志**

```bash
logger -t lab1-check "Lab1 rsyslog test"
```

```bash
sudo tail -n 5 /var/log/syslog
```

期望在最后几行中看到刚才写入的 `Lab1 rsyslog test`。如果两条命令之间输出还没出现，稍等一秒再执行一次 `tail`。

### 步骤 5：验证 VMware Tools 桌面功能

用鼠标拖动 VMware 虚拟机窗口的边缘改变窗口大小。如果 Ubuntu 桌面分辨率能跟着自动适配，说明 VMware Tools 的桌面集成功能正常。

### 验收标准汇总

| 组件 | 验收标准 |
| :--- | :--- |
| VMware Tools | `vmware-toolbox-cmd -v` 输出版本号，`open-vm-tools` 状态为 `active`，窗口缩放时分辨率自动适配 |
| SSH | `ssh` 为 `enabled` 和 `active`，本机正在监听 22 端口 |
| rsyslog | `rsyslog` 为 `enabled` 和 `active`，`/var/log/syslog` 中能找到 `Lab1 rsyslog test` |

### 创建课程基线快照

完成上述检查和 Lab1 作业后，关闭 Ubuntu，在 VMware 中创建一个名为 `Lab1-ready` 的快照。该快照是干净的课程基线：后续实验如果将服务或网络配置改坏，可以回到这个状态。

---

## 十、常见问题

遇到问题时，不要只反复执行 Lab1 中的验收命令。先按下面对应小节修复，再回到 Lab1 重新验证。

### 问题 1：提示 `Unit ssh.service could not be found`

这表示 Ubuntu 没有安装 **SSH 服务端**。`ssh` 客户端和 `openssh-server` 服务端不是一回事：能执行 `ssh` 命令，不代表本机已经能接受 SSH 连接。

先确认软件包是否存在：

```bash
dpkg-query -W openssh-server
```

如果提示没有安装，执行：

```bash
sudo apt update
```

```bash
sudo apt install -y openssh-server
```

安装后立即启动并设为开机自启：

```bash
sudo systemctl enable --now ssh
```

最后确认安装和监听都正常：

```bash
systemctl is-enabled ssh
```

```bash
systemctl is-active ssh
```

```bash
ss -lnt | grep ':22 '
```

三条命令应分别看到 `enabled`、`active` 和包含 `LISTEN` 的 22 端口记录。

### 问题 2：SSH 已安装，但没有启动

先查看完整状态：

```bash
systemctl status ssh --no-pager
```

如果是 `inactive (dead)`，启动它：

```bash
sudo systemctl start ssh
```

如果是 `failed`，不要猜原因，先检查 SSH 配置和最近日志：

```bash
sudo /usr/sbin/sshd -t
```

```bash
sudo journalctl -u ssh -n 30 --no-pager
```

`sshd -t` 没有任何输出表示配置语法正确；如果它显示文件名和行号，按提示修正 `/etc/ssh/sshd_config` 中的错误，然后执行：

```bash
sudo systemctl restart ssh
```

如果日志提示 22 端口已被占用，用下面的命令查看占用该端口的程序：

```bash
sudo ss -lntp | grep ':22 '
```

### 问题 3：SSH 重启后不会自动运行

`active` 只代表现在正在运行，`enabled` 才代表已设为开机自启。执行：

```bash
sudo systemctl enable ssh
```

然后验证：

```bash
systemctl is-enabled ssh
```

期望输出 `enabled`。如果希望一条命令同时完成“现在启动”和“开机自启”，使用：

```bash
sudo systemctl enable --now ssh
```

可执行 `sudo reboot`，重启登录后再运行 `systemctl is-active ssh`，确认输出仍为 `active`。

### 问题 4：SSH 是 `active`，但其他电脑连不上

先在 Ubuntu 中查看 IP 和 22 端口：

```bash
hostname -I
```

```bash
ss -lnt | grep ':22 '
```

然后在 Windows PowerShell 中使用 Ubuntu 的真实用户名和 IP 连接：

```powershell
ssh Ubuntu用户名@Ubuntu的IP地址
```

如果 Ubuntu 的防火墙已启用，需要允许 SSH：

```bash
sudo ufw status
```

只有看到 `Status: active` 时才需要执行：

```bash
sudo ufw allow OpenSSH
```

- 提示 `Connection refused`：通常是 SSH 未运行或 22 端口未监听，按“问题 2”处理。
- 提示 `Connection timed out`：通常是 IP 填错、VMware 网卡未连接、未选 NAT，或防火墙拦截。
- 提示 `Permission denied`：网络和 SSH 服务已经正常，应检查 Ubuntu 用户名和密码；输入密码时终端不显示字符是正常现象。

### 问题 5：VMware Tools 没有安装，或窗口不能自动缩放

Ubuntu 应使用软件源中的 `open-vm-tools`，不需要在 VMware 菜单中挂载旧式 VMware Tools 光盘。先安装服务和桌面集成组件：

```bash
sudo apt update
```

```bash
sudo apt install -y open-vm-tools open-vm-tools-desktop
```

重启服务和虚拟机：

```bash
sudo systemctl restart open-vm-tools
```

```bash
sudo reboot
```

重启登录后验证：

```bash
vmware-toolbox-cmd -v
```

```bash
systemctl is-active open-vm-tools
```

第一条应输出版本号，第二条应输出 `active`。如果仍不能随窗口改变分辨率：

1. 在 VMware 菜单中选择 **View → Autosize → Autofit Guest**。
2. 确认已登录进入 Ubuntu 桌面，而不是停留在登录界面。
3. 执行 `dpkg-query -W open-vm-tools-desktop`，确认桌面组件已安装。
4. 仍有问题时，执行 `sudo apt install --reinstall -y open-vm-tools open-vm-tools-desktop`，然后再次重启。

如果 VMware 菜单中 **Install VMware Tools** 是灰色，但上面两条验证命令正常，不需要处理；Ubuntu 已通过 `open-vm-tools` 提供同等功能。

如果只是复制粘贴或拖放不生效，关闭虚拟机后打开 **VM → Settings → Options → Guest Isolation**，勾选 **Enable copy and paste** 和 **Enable drag and drop**，然后重新启动虚拟机。

### 问题 6：虚拟机没有 IP 或无法联网

1. 关闭虚拟机，在 VMware 的 **VM → Settings → Network Adapter** 中选择 **NAT**，并勾选 **Connected** 和 **Connect at power on**。
2. 启动 Ubuntu 后查看网卡状态：

```bash
nmcli device status
```

3. 如果网卡显示 `disconnected`，先重启 Ubuntu 网络服务：

```bash
sudo systemctl restart NetworkManager
```

4. 再次执行 `hostname -I`、`ip route` 和 `ping -c 4 223.5.5.5`。能 ping IP 但不能 ping 域名时，是 DNS 问题；IP 和域名都不通时，先检查 NAT 和宿主机网络。

### 问题 7：`apt update` 或安装软件失败

- 显示 `Could not resolve`：DNS 或网络未通，先按“问题 6”处理。
- 下载很慢或超时：按第八节换成国内镜像源。
- 换源后显示找不到仓库：Ubuntu 24.04 的 `Suites` 必须是 `noble`，并检查镜像地址是否拼错。
- 显示签名验证失败：不要跳过签名检查，用第八节的备份还原，再重新配置 `Signed-By` 行。
- 修改 `/etc/apt/sources.list` 后没生效：Ubuntu 24.04 要修改的是 `/etc/apt/sources.list.d/ubuntu.sources`。
- 显示 `Could not get lock`：先关闭“软件更新器”，等待另一个 `apt` 任务完成后重试，不要直接删除锁文件。

### 问题 8：没有 `/var/log/syslog` 或 `/var/log/auth.log`

安装 rsyslog，并设为立即启动和开机自启：

```bash
sudo apt install -y rsyslog
```

```bash
sudo systemctl enable --now rsyslog
```

验证状态和写入：

```bash
systemctl is-enabled rsyslog
```

```bash
systemctl is-active rsyslog
```

```bash
logger -t lab1-check "Lab1 rsyslog test"
```

```bash
sudo tail -n 10 /var/log/syslog
```

### 其他常见问题

| 现象 | 可能原因 | 处理 |
| :--- | :--- | :--- |
| VMware 提示不支持虚拟化 | BIOS/UEFI 中未开启虚拟化 | 开启 Intel VT-x / AMD-V / SVM Mode |
| 创建虚拟机后很卡 | 分配内存太少或宿主机内存不足 | 虚拟机至少分配 4GB；关闭宿主机不必要的程序 |
| 忘记 Ubuntu 密码 | 无法使用 `sudo` 或登录 | 刚建好且没有重要数据时，重新安装虚拟机最省时间 |
| 鼠标被虚拟机捕获 | VMware 正常行为 | 按 `Ctrl + Alt` 释放鼠标 |
| 误选中文用户名 | 路径和命令输入可能不方便 | 刚建好时建议重新创建英文小写用户名 |

---

## 十一、完成 Lab1 验收作业

安装和基础组件配置全部完成后，按本目录中的 [`Lab1.md`](Lab1.md) 逐项检查，填写表格并提交要求的截图。

---

## 十二、补充：文件校验

接收大文件后建议校验，确认文件在传输过程中没有损坏。

在 Windows 中按 `Win + X`，选择 **终端** 或 **Windows PowerShell**，用 `cd` 进入存放安装包和镜像的目录，例如：

```powershell
cd D:\LogLab
```

然后执行：

```powershell
Get-FileHash .\ubuntu-24.04.4-desktop-amd64.iso -Algorithm SHA256
Get-FileHash .\VMware-Workstation-Full-26H1-25388281.exe -Algorithm SHA256
Get-FileHash .\VMware-Workstation-Full-26H1-25388281.exe -Algorithm MD5
Get-FileHash .\VMware-Workstation-Full-26H1-25388281.exe -Algorithm SHA1
```

Ubuntu 镜像的 SHA256 应为：

```text
3a4c9877b483ab46d7c3fbe165a0db275e1ae3cfe56a5657e5a47c2f99a99d1e
```

Ubuntu 官方 SHA256 可在这里查看：

<https://releases.ubuntu.com/24.04/SHA256SUMS>

教师发送的 VMware 安装包的校验值为：

```text
MD5 : 83f9b6cc7cf0e74ad3b15ccca27860af
SHA1: 0c892b81cc3519b5159e186c6215b379e5a425ab
```

如果从 Broadcom 官方门户重新下载 VMware 安装包，应以官方门户页面提供的校验值为准。
